<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vipshop.microscope.mysql.dao.MsgReportDao">
	
	<insert id="saveMsgReport" parameterType="MsgReport">
		insert into msg_report
		(id, year, month, week, day, hour, msg_num, msg_size, fail_msg_num, fail_msg_size, fail_msg_pre)
		values
		(#{id},#{year},#{month},#{week},#{day},#{hour},#{msgNum},#{msgSize},#{failMsgNum},#{failMsgSize},#{failMsgPre})
	</insert>
	
</mapper>